# TGB Enterprise — Agent Memory

## Goals (Current Session)
1. ~~Replace `/projects` archive with visual Gallery portfolio at `/gallery`~~ ✓
2. ~~Fix Gallery page visual inconsistency with shared UI/typography~~ ✓
3. ~~Fix animation bug: 8 motion.divs missing variants prop, causing invisible sections~~ ✓
4. ~~Enhance About page content (copy, trust signals, internal links, ContactCTA)~~ ✓
5. ~~Remove section-level lazy loading — sections are lightweight, pages already code-split, Suspense placeholders cause layout shift~~ ✓

## Design Freeze
NO visual redesigns, animations, WebGL, custom cursors, or layout changes unless driven by user feedback, analytics, or performance data.

## Gallery Architecture
- `/gallery` route: dedicated Gallery.tsx page, linked from navigation and Footer
- `/projects` redirects to `/gallery` (react-router Redirect), but `/projects/:projectId` individual detail routes remain active
- Gallery reuses shared `Grid` and `Card` components from `src/components/ui/`
- Section headers use `ServicesOverview.module.css` classes (`.headerBlock`, `.mainTitle`, `.subtitle`)
- All `motion.div` elements using string variants MUST have `variants` prop set to an appropriate variants object

## About Enhancement Summary
- Content rewritten in `src/content/about.ts` across all 12 sections
- Added 5 contextual internal links: introduction → /services, journey → /gallery, whoWeAre → /services, trust → /contact, ourCommitment → /contact
- Metrics marked with `// TODO(Business): Verify this metric before production release.` instead of // REVIEW REQUIRED
- Dated location references updated (e.g. "over 4+ years" → "since Summer 2021")
- Leadership descriptions expanded with specific responsibilities
- Our Commitment: closing paragraph replaced with continuous quality improvement angle


## Tech Stack
- React 18 + TypeScript + Vite (SPA with react-router-dom v6)
- Pre-rendered static HTML per route via `prerender.cjs` (uses puppeteer)
- Sitemap generated by `generate-sitemap.cjs`
- CSS Modules (no Tailwind)
- Fonts: Montserrat (headings) + Roboto (body), loaded via `@fontsource`
- Icons: Lucide React (dynamic, tree-shaken)
- Animations: framer-motion (page transitions + scroll reveals)
- Build: `npm run build` (alias: copy-assets → generate-sitemap → tsc → vite build → prerender)
- Hosting: static export (no SSR)

## Content == Data Pattern
All content lives in `src/content/*.ts` as typed data exports → passed as props to components. Build scripts parse these files with regex — changing data structures silently breaks `generate-sitemap.cjs` or `prerender.cjs`.

## Route Structure
```
/                    → Home
/about               → About (12 content sections + ContactCTA)
/services            → Services index
/services/:slug      → Service detail (dynamic, 6 services)
/gallery             → Gallery portfolio (replaces /projects)
/projects            → Redirect to /gallery
/projects/:id        → Individual project detail (still active)
/contact             → Contact
/claim-warranty      → Warranty claim
/privacy             → Privacy policy
/terms               → Terms of service
```

## Key Conventions
- `SectionEyebrow` component for all orange eyebrow labels
- CSS Modules: `Identity.module.css` for shared About/Home patterns, `ServicesOverview.module.css` for all service section header blocks, per-page `.module.css` for unique styling
- Shared components in `src/components/ui/` (Card, Grid); section composites in `src/components/sections/`
- Animations: `pageTransition` for page enter/exit, `headerVariants` + `staggerContainer` + `fadeInUp` per section
- All images use descriptive `alt` text
- Breadcrumb schema + LocalBusiness JSON-LD + canonical URL set in `AppLayout.tsx`
- Environment variables in `.env` (prefixed with `VITE_`)
